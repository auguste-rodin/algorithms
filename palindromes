// The task is to check whether a string is a palindrome or not, and return true if it is.
// So what you need to do is reverse the string and compare the two with a boolean question.

function isPalindrome(str) {
  const revString = str.split('')
}
